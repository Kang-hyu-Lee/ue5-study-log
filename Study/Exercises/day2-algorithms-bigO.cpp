// Day 2 — DSA Exercises: Algorithm & Big-O Intuition
// No code needed — these are pencil/paper reasoning questions.
// Write your answer as a comment under each question.

// Q1. You have a deck of 52 shuffled cards, face down. You flip them one
// at a time looking for the Ace of Spades. In the worst case, how many
// flips? Which Big-O category is this?
// Your answer:

// Q2. Same deck, but this time it's already sorted by suit and rank, and
// you're allowed to cut the deck in half repeatedly like the phone book
// example. Which Big-O category?
// Your answer:

// Q3. You want to check if any two students in a class of 30 share the
// same birthday, by comparing every student to every OTHER student.
// If class size doubles to 60, does the number of comparisons roughly
// double, or roughly quadruple? Which Big-O category is this?
// Your answer:

// Q4. Accessing the 5th element of an array by its index (myArray[5])
// takes the same number of steps whether the array has 10 elements or
// 10 million. Which Big-O category is this?
// Your answer:

// Q5. Rank these from FASTEST growth (worst) to SLOWEST growth (best)
// as input size increases: O(n), O(log n), O(1), O(n²)
// Your answer:

// Q6. A friend says "my algorithm is O(n²), but it's fine because my
// input is only ever 5 items." Are they right or wrong, and why?
// (Hint: Big-O describes growth rate, not a single measurement — think
// about what happens if their input assumption breaks later.)
// Your answer:
